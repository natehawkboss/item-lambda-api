# Local default — data lives on this machine's disk.
DATABASE_URL=sqlite:///./nextera.db

# Production — data lives over the network, so the servers become disposable.
# DATABASE_URL=postgresql+psycopg://user:pass@host:5432/nextera

DEBUG=false
